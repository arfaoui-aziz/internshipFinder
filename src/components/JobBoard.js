import React from "react";

export default function JobBoard({ internship }) {
  return (
    <div>
      <h1 className="text-2xl font-bold text-red-600">Job Board</h1>

      {console.log(internship)}
    </div>
  );
}
